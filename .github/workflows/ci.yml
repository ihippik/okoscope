name: ci

on:
  pull_request:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  userspace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.91.0
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --exclude agent-ebpf --all-targets -- -D warnings
      - run: cargo test --workspace --exclude agent-ebpf
      - run: cargo check -p protocol

  image-build:
    needs: userspace
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: agent
            dockerfile: Dockerfile.agent
            architecture: amd64
            runner: ubuntu-24.04
          - component: server
            dockerfile: Dockerfile.server
            architecture: amd64
            runner: ubuntu-24.04
          - component: agent
            dockerfile: Dockerfile.agent
            architecture: arm64
            runner: ubuntu-24.04-arm
          - component: server
            dockerfile: Dockerfile.server
            architecture: arm64
            runner: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and optionally publish image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/${{ matrix.architecture }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ghcr.io/${{ github.repository }}-${{ matrix.component }}:${{ github.sha }}-${{ matrix.architecture }}
          cache-from: type=gha,scope=${{ matrix.component }}-${{ matrix.architecture }}
          cache-to: type=gha,mode=max,scope=${{ matrix.component }}-${{ matrix.architecture }}

  image-index:
    if: github.event_name != 'pull_request'
    needs: image-build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        component: [agent, server]
    steps:
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate multi-platform image metadata
        id: metadata
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}-${{ matrix.component }}
          tags: |
            type=raw,value=${{ github.sha }}
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
      - name: Publish multi-platform image index
        env:
          IMAGE: ghcr.io/${{ github.repository }}-${{ matrix.component }}
          SOURCE_TAG: ${{ github.sha }}
          OUTPUT_TAGS: ${{ steps.metadata.outputs.tags }}
        run: |
          tags=()
          while IFS= read -r tag; do
            tags+=(--tag "$tag")
          done <<< "$OUTPUT_TAGS"
          docker buildx imagetools create "${tags[@]}" \
            "$IMAGE:$SOURCE_TAG-amd64" \
            "$IMAGE:$SOURCE_TAG-arm64"

  deployment-manifest:
    if: github.event_name != 'pull_request'
    needs: [userspace, image-index]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Render immutable GHCR image references
        env:
          IMAGE_PREFIX: ghcr.io/${{ github.repository }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          sed \
            -e "s#image: okoscope/server:dev#image: ${IMAGE_PREFIX}-server:${IMAGE_TAG}#" \
            -e "s#image: okoscope/agent:dev#image: ${IMAGE_PREFIX}-agent:${IMAGE_TAG}#" \
            deploy/kubernetes/mvp.yaml > okoscope-mvp.yaml
      - uses: actions/upload-artifact@v4
        with:
          name: okoscope-mvp-${{ github.sha }}
          path: okoscope-mvp.yaml
          if-no-files-found: error
          retention-days: 30
